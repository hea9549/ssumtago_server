<%= form_for(board_comment) do |f| %>
  <% if board_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(board_comment.errors.count, "error") %> prohibited this board_comment from being saved:</h2>

      <ul>
      <% board_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
